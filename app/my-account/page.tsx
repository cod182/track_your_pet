import { WeatherBar } from '@/components';
import FadeIn from 'react-fade-in';

const page = () => {
  return (
    <div className="w-full h-full">
      <WeatherBar />
    </div>
  );
};

export default page;
